# 📦 دليل التثبيت والتشغيل

## المتطلبات

لا توجد متطلبات خاصة! كل ما تحتاجه هو:
- متصفح ويب حديث (Chrome, Firefox, Safari, Edge)
- لا حاجة لتثبيت أي برامج إضافية

## طرق التشغيل

### الطريقة 1️⃣: فتح الملف مباشرة (الأسهل)

1. **فك ضغط الملف:**
   - انقر بزر الماوس الأيمن على ملف `animal_quiz_html.zip`
   - اختر "استخراج" أو "Extract"

2. **فتح الموقع:**
   - انقر نقراً مزدوجاً على ملف `index.html`
   - سيفتح الموقع تلقائياً في متصفحك الافتراضي

### الطريقة 2️⃣: استخدام خادم محلي (موصى به للأداء الأفضل)

#### على Windows:

**باستخدام Command Prompt:**
```bash
cd path\to\animal_quiz_html
python -m http.server 8000
```

ثم افتح المتصفح وانتقل إلى: `http://localhost:8000`

#### على Mac:

**باستخدام Terminal:**
```bash
cd path/to/animal_quiz_html
python3 -m http.server 8000
```

ثم افتح المتصفح وانتقل إلى: `http://localhost:8000`

#### على Linux:

```bash
cd path/to/animal_quiz_html
python3 -m http.server 8000
```

ثم افتح المتصفح وانتقل إلى: `http://localhost:8000`

### الطريقة 3️⃣: استخدام Live Server (إذا كان مثبتاً)

إذا كان لديك Live Server مثبتاً:
```bash
cd path/to/animal_quiz_html
live-server
```

## 🎮 البدء باللعب

1. **الشاشة الأولى:** ستظهر جميع الحيوانات المتاحة
2. **اضغط على الزر الأخضر:** "ابدأ المسابقة! 🚀"
3. **اجب على الأسئلة:** اختر الإجابة الصحيحة من بين 4 خيارات
4. **استمع للمؤثرات الصوتية:** ستسمع صوتاً مختلفاً لكل إجابة
5. **شاهد نتيجتك:** بعد الانتهاء، ستظهر نتيجتك النهائية

## ⚙️ استكشاف الأخطاء والمشاكل

### المشكلة: الموقع لا يفتح

**الحل:**
- تأكد من أن جميع الملفات في نفس المجلد
- جرب متصفح مختلف
- استخدم خادم محلي بدلاً من فتح الملف مباشرة

### المشكلة: المؤثرات الصوتية لا تعمل

**الحل:**
- تأكد من أن صوت جهازك غير مكتوم
- جرب متصفح مختلف
- على الهواتف الذكية، قد تحتاج للسماح بتشغيل الأصوات

### المشكلة: الموقع بطيء

**الحل:**
- استخدم خادم محلي بدلاً من فتح الملف مباشرة
- امسح ذاكرة التخزين المؤقت للمتصفح
- أغلق البرامج الأخرى التي تستهلك موارد النظام

## 📱 التوافقية

| المتصفح | الإصدار | التوافقية |
|---------|---------|----------|
| Chrome | 90+ | ✅ ممتاز |
| Firefox | 88+ | ✅ ممتاز |
| Safari | 14+ | ✅ ممتاز |
| Edge | 90+ | ✅ ممتاز |
| Opera | 76+ | ✅ ممتاز |

## 🔒 الأمان والخصوصية

- ✅ لا يتم جمع أي بيانات شخصية
- ✅ لا يتم الاتصال بأي خادم خارجي
- ✅ يعمل بشكل كامل بدون اتصال بالإنترنت
- ✅ آمن تماماً للأطفال

## 📝 ملاحظات مهمة

- الموقع محسّن بالكامل للأطفال الصغار
- جميع النصوص باللغة العربية
- الموقع يعمل على جميع الأجهزة (هواتف، أجهزة لوحية، حواسيب)
- يمكنك تعديل الأسئلة والألوان حسب احتياجاتك

## 🆘 هل تحتاج إلى مساعدة؟

إذا واجهت أي مشاكل:
1. تحقق من أن جميع الملفات موجودة
2. جرب متصفح مختلف
3. امسح ذاكرة التخزين المؤقت للمتصفح
4. استخدم خادم محلي

---

**استمتع باللعب والتعلم! 🎉**
